[request_definition]
r = sub, obj, act

[policy_definition]
p = sub_rule, obj, act

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m.0 = eval(p.sub_rule)
m.1 = r.obj == p.obj && r.act == p.act